# Next Berlin work

## First proof delivered

Real scheduled Ringbahn and selected crossing modes; source-pinned importer; complete directed shape coverage within the chosen bounds; geographic/circulation transition; desktop and phone controls; source attribution and lineage. The Ostkreuz relative interchange now adds platform levels, source connector filters and scheduled calls on the same clock. The proof remains unnumbered.

## Next source and composition gates

1. The author approved the geographic/circulation transition. Two-direction inspection and authored outer/inner spacing are implemented. The reversible city-to-Ostkreuz camera approach and relative-level arrival are implemented, together with station names that emerge at closer zoom. Next, review the complete opening with the author: whether the Ring visibly makes an inside, and whether the crossing scene complicates it clearly enough. Decide whether Umlauf becomes the final title.
2. Obtain engineering or classified survey evidence for absolute rail heights and individual platform endpoints. The [engineering review](ENGINEERING.md) now establishes cited net built lengths and platform height above rail, with close inspection controls; it does not establish a vertical datum or approach profile. The [ATKIS audit](ATKIS.md) now retains official rail/bridge records and renders the linked Ostkreuz bridge footprint, but establishes no metre heights. GTFS levels/pathways remain the source of relative ordering; ground and roof elevations must remain distinct from track.
3. Official water context is implemented with retained source bytes, exact requests, CRS and clipping history in [WATER.md](WATER.md). Terrain is deferred; bare-earth height would not settle rail-deck heights.
4. Review source simplification visually at close zoom, and investigate the excluded Lichtenrade shape mismatch if the southern scope expands. Validate calendar boundaries and previous-service-day trips explicitly before changing the opening to overnight hours.
5. Measure frame rate, memory and transfer on physical phones. Current checks are desktop/phone browser emulation and payload accounting. The shared flat-map fixes and quiet-ground option are published upstream in alpha.3 and consumed by this preview. [PERFORMANCE.md](PERFORMANCE.md) defines the device protocol and payload gate.
6. Decide publication and catalogue admission after review. A manual checked Pages workflow is prepared; the private repository plan blocked its initial activation. The owner-only Sites source upload and private preview were explicitly approved. Public Pages publication is approved. The initial deployment used a separate repository; the canonical site now uses this repository’s manual checked workflow at https://emmettl.github.io/umlauf/. The source repository and full Git history are also now public following separate explicit approval; see [PUBLISHING.md](PUBLISHING.md). Expand to a full day only after the bounded opening survives these checks.

Realtime is optional: the previous VBB audit found trip updates with limited coverage, not continuous vehicle positions. It is not a dependency of the current scheduled work.

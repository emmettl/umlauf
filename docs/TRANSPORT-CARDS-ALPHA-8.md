# Shared transport cards · alpha.8

All consumed `@motionstudies/*` packages are pinned to `0.1.0-alpha.8` with npm registry integrity hashes.

Selected map stations show a matrix rail card. The board uses departure times, omits terminal arrivals, retains repeated calls and highlights the selected journey. The existing station-close control remains.

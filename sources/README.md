# Local source archive

Ignored files retained here: `vbb-20260903.zip` (the reviewed 75 MB GTFS archive) and `vbb-source-page.html` (the licence/source page). See `../docs/licence-receipt.json` and `../docs/DATA.md` for hashes and acquisition history.

The repo includes compiled runtime data and provenance, so running the app does not require these files. Recompilation does. Preserve a durable copy before cleaning this directory; the live VBB download URL can change.
